# Environment
.env

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
*.egg-info/
dist/
build/
.Python

# Data — PDFs crudos (pesados, se regeneran con el pipeline)
data/dfi_pdfs/
data/dfi_pdfsl/

# La base de datos (funds.db y chroma/) se incluye en el repo intencionalmente
# para que la imagen Docker pueda arrancar sin ejecutar el pipeline de extracción.
# Ver README sección "Base de datos" para más detalles.

# Evaluation cache (contiene respuestas generadas, no código fuente)
evaluation/responses_cache.json

# macOS
.DS_Store

# IDEs
.idea/
.vscode/
*.swp

# Claude Code internal
.claude/
